# Accepted Core Fixture

Package: ail.std.core.

Function: Identity.copy.

The function needs:

- value: T

The function produces:

- result: T

When Identity.copy runs:

- the function returns value unchanged
- the function records a trace event named IdentityCopied
